How to create a microservice

//use below commands to create folders

dotnet new webapi -n Expense.API
dotnet new classlib -n Expense.Application
dotnet new classlib -n Expense.Domain
dotnet new classlib -n Expense.Infrastructure

//Add references 

dotnet add Expense.API reference Expense.Application
dotnet add Expense.API reference Expense.Infrastructure

dotnet add Expense.Application reference Expense.Domain

dotnet add Expense.Infrastructure reference Expense.Application


//Add ef core package, if error thorws add with specific package

dotnet add Expense.Infrastructure package Microsoft.EntityFrameworkCore
dotnet add Expense.Infrastructure package Microsoft.EntityFrameworkCore.SqlServer

//Add connection string in appsettings.json

//TO run migrations at first to create a new table in DB, add context in Repository and create model/entities in domain(dto)

//Add tbcontext injection in API layer in program.classlib

using User.Infrastructure.Repository;
using Microsoft.EntityFrameworkCore;

var builder = WebApplication.CreateBuilder(args);

// Add DbContext
builder.Services.AddDbContext<ExpenseRepository>(options =>
    options.UseNpgsql(builder.Configuration.GetConnectionString("DefaultConnection"))
);

var app = builder.Build();

app.UseSwagger();
app.UseSwaggerUI();
app.MapControllers();

app.Run();

//run below commands to apply migrations

dotnet ef migrations add AddUserTable --project Expense.Infrastructure --startup-project Expense.API
dotnet ef database update --project Expense.Infrastructure --startup-project Expense.API


steps to write a simple API,(personal use)

1.create dto/model/entities in domain if needed
2.create interface and add your method there
3.create service and implement this interface and query using LINQ
4.go to application layer and based on CQRS pattern create a get.cs file and inject Interface there and call method inside that service
5.map this inteface and infra sevrice in program.cs
6.go to controller and inject query file from application and call the method

// to add foreign key reference, a microservice should have both the models, if need to use same model from another
microservice means, add that model in shared.Model something, right now i dont want want that , so that i have  created in a single microservice itself

//navigation property , parent to child is ok for navigation , but reverse navigation is child to parent is not ok, instead we can use joins

//normal
namespace User.Domain.Models
{
    public class UserDto
    {
        public int Id { get; set; }

        public string Name { get; set; } = null!;

        public int Mobile { get; set; }

        public ICollection<Expense> Expenses { get; set; } = new List<Expense>();
    }
}

//reverse 
namespace Expense.Domain.Models
{
    public class Expense
    {
        public int Id { get; set; }

        // Foreign key
        public int UserId { get; set; }

        public string Category { get; set; } = string.Empty;
        public decimal Amount { get; set; }
        public DateTime Date { get; set; }
        public string? Note { get; set; }

        // Navigation property
        public User User { get; set; } = null!;
    }
}

//Factory pattern -> it is like which object should created at runtime (by hiding unwanted details from consumer)

//Strategy pattern -> it is like which logic should be used at runtime(object already created)

//both are almost similar, simple point is , factory should use if/else , strategy we will assign context manually

//whenever ur creating some new class and need ot use in a microservice means add reference

//sealed – stops a class from being inherited; design ends here.
//init – allows setting a property only during object creation, not later.
//protected (method) – method accessible only inside the class and its child classes.
//[Serializable] – marks a class so it can be converted to bytes (store, send, cache).
//RequestDelegate – represents the next middleware call in the ASP.NET Core pipeline.
//ILogger – standard interface to write structured logs (info, warning, error).

//created identity service as a separate microsevrice and added login and register POST APIs
//JWT comfiguration should be added in IdentityService appsetttings.json

//Authentication and authorization should be handled in API gateway ,
 --> like wheni try to hit microservice it should have 
      "AuthenticationOptions": {
        "AuthenticationProviderKey": "Bearer",
        "AllowedScopes": []
      }

      if so it should check the bearer token in header, if it is invalid, it will throw 401 unauthorized

//Authentication — verifies identity using credentials or tokens; failure typically returns 401 (Unauthenticated).
//Authorization — verifies permissions or access level after identity is confirmed; failure typically returns 403 (Forbidden)

//Created a shared Redis library 
            -> it has two one is interface (contract) and other is Service which will get data from redis DB (like a translator + courier between your app and Upstash Redis)

//Also we created a method and table ref in infra, if cache is not there means, it will hit db and then it will save data in redis too
//Used Upstash Online redis service viw REST way

//Mediator Pattern (MediatR)

//Mediator pattern is used to reduce tight coupling between controller and business logic.
//Instead of directly injecting Commands/Queries into controller,
//controller will communicate only with IMediator.

//Before mediator

Controller
   -> Query / Command
        -> Service
            -> Repository

//After mediator

Controller
   -> IMediator
        -> Handler
             -> Service
                  -> Repository

//Main advantage:
//controller becomes very thin and clean

//Before

private readonly GetAllExpenseQuery _getAllExpenseQuery;
private readonly CreateExpenseCommand _createExpenseCommand;
private readonly UpdateExpenseCommand _updateExpenseCommand;

//After

private readonly IMediator _mediator;

//Mediator automatically finds correct handler and executes it.

//Install packages

//API Layer
dotnet add package MediatR
dotnet add package MediatR.Extensions.Microsoft.DependencyInjection

//Application Layer
dotnet add package MediatR

//Register mediator in Program.cs

using MediatR;
using System.Reflection;

builder.Services.AddMediatR(cfg =>
{
    cfg.RegisterServicesFromAssembly(Assembly.Load("Expense.Application"));
});

//------------------------------------------------------------

//IRequest<T>

//IRequest<T> represents a request sent through mediator
//T represents the response type

//Syntax

IRequest<T>

//Example

public class GetAllExpenseQuery : IRequest<List<ExpenseDto>>
{
    public string? SearchTerm { get; set; }
}

//Meaning:
//GetAllExpenseQuery will return List<ExpenseDto>

//Examples

IRequest<int>
//returns integer

IRequest<UserDto>
//returns UserDto

IRequest<List<ExpenseDto>>
//returns list of expenses

//------------------------------------------------------------

//IRequestHandler<TRequest, TResponse>

//This handles/processes the request

//Syntax

IRequestHandler<RequestType, ReturnType>

//Example

public class GetAllExpenseQueryHandler
    : IRequestHandler<GetAllExpenseQuery, List<ExpenseDto>>
{
}

//Meaning:
//This handler processes GetAllExpenseQuery
//and returns List<ExpenseDto>

//Mediator internally matches request with correct handler

//Example

GetAllExpenseQuery
      ->
GetAllExpenseQueryHandler

//------------------------------------------------------------

//Handle() method

//Handle method contains actual business logic
//Mediator automatically calls this method internally

//Syntax

public async Task<TResponse> Handle(
    TRequest request,
    CancellationToken cancellationToken
)
{
}

//Example

public async Task<List<ExpenseDto>> Handle(
    GetAllExpenseQuery request,
    CancellationToken cancellationToken
)
{
    var query = _expenseService.GetAllExpenses();

    return query.ToList();
}

//Meaning:
//When mediator receives GetAllExpenseQuery,
//it automatically calls Handle() method of matching handler

//------------------------------------------------------------

//Mediator Workflow

Controller
    ->
_mediator.Send(new GetAllExpenseQuery())
    ->
Mediator searches matching handler
    ->
GetAllExpenseQueryHandler
    ->
Handle() method executes
    ->
Service
    ->
Database

//------------------------------------------------------------

//Visual representation

┌────────────────────┐
│ ExpenseController  │
└─────────┬──────────┘
          │
          │ Send(Query)
          ▼
┌────────────────────┐
│     IMediator      │
└─────────┬──────────┘
          │
          │ Finds matching handler
          ▼
┌─────────────────────────────┐
│ GetAllExpenseQueryHandler   │
└─────────┬───────────────────┘
          │
          │ Calls service
          ▼
┌────────────────────┐
│   ExpenseService   │
└─────────┬──────────┘
          │
          ▼
┌────────────────────┐
│     Database       │
└────────────────────┘

//------------------------------------------------------------

//Controller usage

var result = await _mediator.Send(
    new GetAllExpenseQuery()
);

//builder.Services.AddMediatR(cfg =>
//{
//    cfg.RegisterServicesFromAssembly(Assembly.Load("Expense.Application"));
//});

//This code registers MediatR and tells it:

//"Go to Expense.Application assembly/project
//and automatically find all handlers"

//Mediator internally:
//1. Finds matching handler
//2. Creates handler object using DI
//3. Calls Handle()
//4. Returns response

//------------------------------------------------------------

//Benefits
//1. Loose coupling
//2. Cleaner controller
//3. Better scalability
//4. Easier testing
//5. Easy to add validation/logging globally using pipeline behaviors

//------------------------------------------------------------

//CQRS + Mediator

//Command -> used for create/update/delete
//Query -> used for fetch/read operations