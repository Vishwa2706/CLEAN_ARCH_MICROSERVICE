How to create a microservice

//use below commands to create folders

dotnet new webapi -n Expense.API
dotnet new classlib -n Expense.Application
dotnet new classlib -n Expense.Domain
dotnet new classlib -n Expense.Infrastructure

//Add references 

dotnet add Expense.API reference Expense.Application
dotnet add Expense.API reference Expense.Infrastructure

dotnet add Expense.Application reference Expense.Domain

dotnet add Expense.Infrastructure reference Expense.Application


//Add ef core package, if error thorws add with specific package

dotnet add Expense.Infrastructure package Microsoft.EntityFrameworkCore
dotnet add Expense.Infrastructure package Microsoft.EntityFrameworkCore.SqlServer

//Add connection string in appsettings.json

//TO run migrations at first to create a new table in DB, add context in Repository and create model/entities in domain(dto)

//Add tbcontext injection in API layer in program.classlib

using User.Infrastructure.Repository;
using Microsoft.EntityFrameworkCore;

var builder = WebApplication.CreateBuilder(args);

// Add DbContext
builder.Services.AddDbContext<ExpenseRepository>(options =>
    options.UseNpgsql(builder.Configuration.GetConnectionString("DefaultConnection"))
);

var app = builder.Build();

app.UseSwagger();
app.UseSwaggerUI();
app.MapControllers();

app.Run();

//run below commands to apply migrations

dotnet ef migrations add AddUserTable --project Expense.Infrastructure --startup-project Expense.API
dotnet ef database update --project Expense.Infrastructure --startup-project Expense.API


steps to write a simple API,(personal use)

1.create dto/model/entities in domain if needed
2.create interface and add your method there
3.create service and implement this interface and query using LINQ
4.go to application layer and based on CQRS pattern create a get.cs file and inject Interface there and call method inside that service
5.map this inteface and infra sevrice in program.cs
6.go to controller and inject query file from application and call the method

// to add foreign key reference, a microservice should have both the models, if need to use same model from another
microservice means, add that model in shared.Model something, right now i dont want want that , so that i have  created in a single microservice itself

//navigation property , parent to child is ok for navigation , but reverse navigation is child to parent is not ok, instead we can use joins

//normal
namespace User.Domain.Models
{
    public class UserDto
    {
        public int Id { get; set; }

        public string Name { get; set; } = null!;

        public int Mobile { get; set; }

        public ICollection<Expense> Expenses { get; set; } = new List<Expense>();
    }
}

//reverse 
namespace Expense.Domain.Models
{
    public class Expense
    {
        public int Id { get; set; }

        // Foreign key
        public int UserId { get; set; }

        public string Category { get; set; } = string.Empty;
        public decimal Amount { get; set; }
        public DateTime Date { get; set; }
        public string? Note { get; set; }

        // Navigation property
        public User User { get; set; } = null!;
    }
}

//Factory pattern -> it is like which object should created at runtime (by hiding unwanted details from consumer)

//Strategy pattern -> it is like which logic should be used at runtime(object already created)

//both are almost similar, simple point is , factory should use if/else , strategy we will assign context manually

//whenever ur creating some new class and need ot use in a microservice means add reference

//sealed – stops a class from being inherited; design ends here.
//init – allows setting a property only during object creation, not later.
//protected (method) – method accessible only inside the class and its child classes.
//[Serializable] – marks a class so it can be converted to bytes (store, send, cache).
//RequestDelegate – represents the next middleware call in the ASP.NET Core pipeline.
//ILogger – standard interface to write structured logs (info, warning, error).

//created identity service as a separate microsevrice and added login and register POST APIs
//JWT comfiguration should be added in IdentityService appsetttings.json

//Authentication and authorization should be handled in API gateway ,
 --> like wheni try to hit microservice it should have 
      "AuthenticationOptions": {
        "AuthenticationProviderKey": "Bearer",
        "AllowedScopes": []
      }

      if so it should check the bearer token in header, if it is invalid, it will throw 401 unauthorized

//When a request comes in, the JwtBearer middleware extracts the token and passes it to JwtSecurityTokenHandler.
//The handler decodes the token, verifies the signature using the signing key, and then applies validations like issuer, audience, and lifetime based on TokenValidationParameters.
//If any validation fails, it throws an exception and returns 401

//Authentication — verifies identity using credentials or tokens; failure typically returns 401 (Unauthenticated).
//Authorization — verifies permissions or access level after identity is confirmed; failure typically returns 403 (Forbidden)

//Created a shared Redis library 
            -> it has two one is interface (contract) and other is Service which will get data from redis DB (like a translator + courier between your app and Upstash Redis)

//Also we created a method and table ref in infra, if cache is not there means, it will hit db and then it will save data in redis too
//Used Upstash Online redis service viw REST way

//Mediator Pattern (MediatR)

//Mediator pattern is used to reduce tight coupling between controller and business logic.
//Instead of directly injecting Commands/Queries into controller,
//controller will communicate only with IMediator.

//Before mediator

Controller
   -> Query / Command
        -> Service
            -> Repository

//After mediator

Controller
   -> IMediator
        -> Handler
             -> Service
                  -> Repository

//Main advantage:
//controller becomes very thin and clean

//Before

private readonly GetAllExpenseQuery _getAllExpenseQuery;
private readonly CreateExpenseCommand _createExpenseCommand;
private readonly UpdateExpenseCommand _updateExpenseCommand;

//After

private readonly IMediator _mediator;

//Mediator automatically finds correct handler and executes it.

//Install packages

//API Layer
dotnet add package MediatR
dotnet add package MediatR.Extensions.Microsoft.DependencyInjection

//Application Layer
dotnet add package MediatR

//Register mediator in Program.cs

using MediatR;
using System.Reflection;

builder.Services.AddMediatR(cfg =>
{
    cfg.RegisterServicesFromAssembly(Assembly.Load("Expense.Application"));
});

//------------------------------------------------------------

//IRequest<T>

//IRequest<T> represents a request sent through mediator
//T represents the response type

//Syntax

IRequest<T>

//Example

public class GetAllExpenseQuery : IRequest<List<ExpenseDto>>
{
    public string? SearchTerm { get; set; }
}

//Meaning:
//GetAllExpenseQuery will return List<ExpenseDto>

//Examples

IRequest<int>
//returns integer

IRequest<UserDto>
//returns UserDto

IRequest<List<ExpenseDto>>
//returns list of expenses

//------------------------------------------------------------

//IRequestHandler<TRequest, TResponse>

//This handles/processes the request

//Syntax

IRequestHandler<RequestType, ReturnType>

//Example

public class GetAllExpenseQueryHandler
    : IRequestHandler<GetAllExpenseQuery, List<ExpenseDto>>
{
}

//Meaning:
//This handler processes GetAllExpenseQuery
//and returns List<ExpenseDto>

//Mediator internally matches request with correct handler

//Example

GetAllExpenseQuery
      ->
GetAllExpenseQueryHandler

//------------------------------------------------------------

//Handle() method

//Handle method contains actual business logic
//Mediator automatically calls this method internally

//Syntax

public async Task<TResponse> Handle(
    TRequest request,
    CancellationToken cancellationToken
)
{
}

//Example

public async Task<List<ExpenseDto>> Handle(
    GetAllExpenseQuery request,
    CancellationToken cancellationToken
)
{
    var query = _expenseService.GetAllExpenses();

    return query.ToList();
}

//Meaning:
//When mediator receives GetAllExpenseQuery,
//it automatically calls Handle() method of matching handler

//------------------------------------------------------------

//Mediator Workflow

Controller
    ->
_mediator.Send(new GetAllExpenseQuery())
    ->
Mediator searches matching handler
    ->
GetAllExpenseQueryHandler
    ->
Handle() method executes
    ->
Service
    ->
Database

//------------------------------------------------------------

//Visual representation

┌────────────────────┐
│ ExpenseController  │
└─────────┬──────────┘
          │
          │ Send(Query)
          ▼
┌────────────────────┐
│     IMediator      │
└─────────┬──────────┘
          │
          │ Finds matching handler
          ▼
┌─────────────────────────────┐
│ GetAllExpenseQueryHandler   │
└─────────┬───────────────────┘
          │
          │ Calls service
          ▼
┌────────────────────┐
│   ExpenseService   │
└─────────┬──────────┘
          │
          ▼
┌────────────────────┐
│     Database       │
└────────────────────┘

//------------------------------------------------------------

//Controller usage

var result = await _mediator.Send(
    new GetAllExpenseQuery()
);

//builder.Services.AddMediatR(cfg =>
//{
//    cfg.RegisterServicesFromAssembly(Assembly.Load("Expense.Application"));
//});

//This code registers MediatR and tells it:

//"Go to Expense.Application assembly/project
//and automatically find all handlers"

//Mediator internally:
//1. Finds matching handler
//2. Creates handler object using DI
//3. Calls Handle()
//4. Returns response

//------------------------------------------------------------

//Benefits
//1. Loose coupling
//2. Cleaner controller
//3. Better scalability
//4. Easier testing
//5. Easy to add validation/logging globally using pipeline behaviors

//------------------------------------------------------------

//CQRS + Mediator

//Command -> used for create/update/delete
//Query -> used for fetch/read operations

//------------------------------------------------------------

//API Gateway using YARP (Full Flow Guide)

//Create API Gateway project

dotnet new webapi -n APIGateway.YARP

//------------------------------------------------------------

//Install required packages

dotnet add package Yarp.ReverseProxy
dotnet add package System.IdentityModel.Tokens.Jwt

//------------------------------------------------------------

//Create folders

Contracts
Infrastructure (Authentication, Services)
Extensions

//------------------------------------------------------------

//Token validation contract

public interface ITokenValidator
{
    Task<(bool IsValid, JwtSecurityToken JwtToken, string Error)> ValidateAsync(string token);
}

//------------------------------------------------------------

//Token validation service flow

//1. Read token
//2. Check token format
//3. Check expiry
//4. (Future) call IdentityService to validate from DB

//------------------------------------------------------------

//Authentication Handler (core logic)

//Flow

Client Request
    ->
Gateway receives request
    ->
Check Authorization header
    ->
Extract Bearer token
    ->
Call TokenValidator
    ->
If invalid -> return 401
    ->
If valid -> extract claims
    ->
Create ClaimsIdentity
    ->
Attach to HttpContext.User
    ->
Return success

//------------------------------------------------------------

//Claims

//Claims are taken from JWT token

//Example

sub -> UserId
email -> Email
role -> Role

//Claims will be stored in

HttpContext.User

//------------------------------------------------------------

//Authorization Policy

//Used in YARP routes

"AuthorizationPolicy": "ApiTokenPolicy"

//Means:
//Request must be authenticated before forwarding

//------------------------------------------------------------

//Register services in Program.cs

builder.Services.AddSingleton<ITokenValidator, JwtTokenValidator>();

builder.Services.AddAuthentication("ApiTokenScheme")
    .AddScheme<AuthenticationSchemeOptions, ApiTokenHandler>(
        "ApiTokenScheme", null);

builder.Services.AddAuthorization(options =>
{
    options.AddPolicy("ApiTokenPolicy", policy =>
        policy.RequireAuthenticatedUser());
});

//------------------------------------------------------------

//Middleware order (very important)

app.UseAuthentication();
app.UseAuthorization();

app.MapReverseProxy();

//------------------------------------------------------------

//YARP concepts

//Routes -> incoming request mapping
//Clusters -> destination services

//------------------------------------------------------------

//YARP request flow

Client
    ->
Gateway URL
    ->
Route match (based on path)
    ->
Authorization policy check
    ->
Authentication handler runs
    ->
Token validation
    ->
Claims creation
    ->
Cluster selected
    ->
Forward request to microservice
    ->
Response returned

//------------------------------------------------------------

//Public vs Secure routes

//Public routes (no token required)
//Example
/api/identity/v1/auth/login

//Secure routes (token required)
//Example
/api/client/v1/{everything}

//------------------------------------------------------------

//Transforms

//Used to change request path

//Example

/api/client/v1/test
    ->
/client/v1/test

//------------------------------------------------------------

//Development vs Production config

//Development
appsettings.Development.json -> localhost services

//Production
appsettings.yarp.json -> Kubernetes services

//------------------------------------------------------------

//Full Gateway Flow

Client
  ->
API Gateway
  ->
Authentication Handler
  ->
Token validation
  ->
Claims extraction
  ->
Authorization check
  ->
Route matching
  ->
Cluster selection
  ->
Forward to service
  ->
Response to client

//------------------------------------------------------------

//Future enhancements

//1. IdentityService token validation (DB check)
//2. Role based authorization
//3. Redis caching
//4. Logging
//5. Rate limiting

//------------------------------------------------------------

//Debug claims

return Ok(User.Claims);

//------------------------------------------------------------

//Key understanding

//JWT -> contains claims
//Gateway -> validates token
//Gateway -> attaches claims
//Service -> reads claims

//------------------------------------------------------------

//CancellationToken

//CancellationToken is used to stop/cancel an operation
//when the request is no longer needed.

//Why needed?

//Sometimes:
//1. User closes browser
//2. Request timeout happens
//3. Client disconnects
//4. Long DB query still running

//Without CancellationToken:
//DB/API operation still continues
//Server resources get wasted

//With CancellationToken:
//ASP.NET Core can stop operation gracefully

//------------------------------------------------------------

//Main Benefits

//1. Prevents unnecessary work
//2. Saves server resources
//3. Improves scalability
//4. Industry standard in async APIs
//5. Useful for DB/API/file operations

//------------------------------------------------------------

//Request flow

Client
   ->
Controller
   ->
Mediator
   ->
Handler
   ->
Service
   ->
EF Core / Database

//CancellationToken flows through all layers

//------------------------------------------------------------

//ASP.NET Core automatically creates token using

HttpContext.RequestAborted

//If client disconnects,
//token gets cancelled automatically

//------------------------------------------------------------

//Controller example

[HttpGet]
public async Task<IActionResult> GetAll(
    CancellationToken cancellationToken
)
{
    var result = await _mediator.Send(
        new GetAllExpenseQuery(),
        cancellationToken
    );

    return Ok(result);
}

//------------------------------------------------------------

//Handler example

public async Task<List<ExpenseDto>> Handle(
    GetAllExpenseQuery request,
    CancellationToken cancellationToken
)
{
    return await _expenseService.GetAllExpensesAsync(
        cancellationToken
    );
}

//------------------------------------------------------------

//Service example

public async Task<List<ExpenseDto>> GetAllExpensesAsync(
    CancellationToken cancellationToken
)
{
    return await _context.Expenses
        .AsNoTracking()
        .ToListAsync(cancellationToken);
}

//------------------------------------------------------------

//EF Core methods supporting CancellationToken

ToListAsync(cancellationToken)

FirstOrDefaultAsync(cancellationToken)

AnyAsync(cancellationToken)

SaveChangesAsync(cancellationToken)

FindAsync([id], cancellationToken)

//------------------------------------------------------------

//Important understanding

//CancellationToken does NOT make query faster directly

//It improves:
//1. Resource usage
//2. Scalability
//3. Thread utilization
//4. Graceful cancellation

//------------------------------------------------------------

//Best practice

//Pass token through all async layers

Controller
   ->
Handler
   ->
Service
   ->
Repository
   ->
Database

//------------------------------------------------------------

//Always keep token as last parameter

Task UpdateAsync(
    int id,
    ExpenseDto dto,
    CancellationToken cancellationToken
);

//------------------------------------------------------------

//Do NOT pass token inside command/query model constructor

//Wrong

new UpdateExpenseCommand(
    id,
    request,
    cancellationToken
)

//Correct

await _mediator.Send(
    new UpdateExpenseCommand(id, request),
    cancellationToken
)
//------------------------------------------------------------

//Parquet File Export

Visual representation

Parquet (column-based)
Id       -> [1,2,3]

Category -> [Food,Travel,Food]

Amount   -> [500,800,200]

//Parquet is a column-based binary file format
//mainly used in analytics and big-data systems.

//Unlike CSV:
//CSV stores row by row

//Example:
//1,Food,500
//2,Travel,700

//Parquet stores column by column

//Id -> 1,2
//Category -> Food,Travel
//Amount -> 500,700

//Because of this:
//1. Better compression
//2. Faster analytical queries
//3. Less storage usage
//4. Schema support
//5. Widely used in Spark/Hadoop/Data Lakes

//------------------------------------------------------------

//Install package

//Infrastructure layer

dotnet add package Parquet.Net

//------------------------------------------------------------

//Exporter contract

public interface IExpenseExporter
{
byte[] Export(IEnumerable<ExpenseDto> expenses);

```
string ContentType { get; }

string FileExtension { get; }
```

}

//------------------------------------------------------------

//Parquet exporter implementation

using Expense.Application.Contracts;
using Expense.Domain.Models;
using Parquet.Serialization;

namespace Expense.Infrastructure.Exporters
{
public class ParquetExpenseExporter : IExpenseExporter
{
public string ContentType => "application/vnd.apache.parquet";

```
    public string FileExtension => "parquet";

    public byte[] Export(IEnumerable<ExpenseDto> expenses)
    {
        using var stream = new MemoryStream();

        ParquetSerializer
            .SerializeAsync(expenses, stream)
            .GetAwaiter()
            .GetResult();

        return stream.ToArray();
    }
}
```

}

//------------------------------------------------------------

//Register exporter in Program.cs

builder.Services.AddScoped<IExpenseExporter, CsvExpenseExporter>();

builder.Services.AddScoped<IExpenseExporter, JsonExpenseExporter>();

builder.Services.AddScoped<IExpenseExporter, ParquetExpenseExporter>();

//------------------------------------------------------------

//Factory Pattern usage

//Factory dynamically creates correct exporter at runtime

//Example

var exporter = _expenseExporterFactory.Create(type);

//Possible values:
//csv
//json
//parquet

//------------------------------------------------------------

//Export API example

[HttpGet("export/{type}")]
public async Task<IActionResult> Export(string type)
{
var expenses = await _mediator.Send(new GetAllExpenseQuery());

```
var exporter = _expenseExporterFactory.Create(type);

var fileBytes = exporter.Export(expenses);

return File(
    fileBytes,
    exporter.ContentType,
    $"expenses.{exporter.FileExtension}"
);
```

}

//------------------------------------------------------------

//Returned files

//GET /export/csv
//returns expenses.csv

//GET /export/json
//returns expenses.json

//GET /export/parquet
//returns expenses.parquet

//------------------------------------------------------------

//File() method

//File() returns downloadable binary/file response

//Unlike:

return Ok(data);

//which returns JSON response

//------------------------------------------------------------

//Parquet advantages over CSV

//CSV
//1. Human readable
//2. Simple
//3. Larger file size
//4. Slower for analytics

//Parquet
//1. Binary format
//2. Smaller file size
//3. Faster reads
//4. Better compression
//5. Better for big datasets

//------------------------------------------------------------

//Common usage areas

//1. Data Lakes
//2. ETL pipelines
//3. Analytics systems
//4. Machine Learning datasets
//5. Spark/Hadoop processing

//------------------------------------------------------------

//Important understanding

//Exporter converts data -> byte[]

//CSV exporter -> text bytes
//JSON exporter -> json bytes
//Parquet exporter -> parquet binary bytes

//ASP.NET Core File() method sends these bytes as downloadable file

//------------------------------------------------------------

//FluentValidation + MediatR Pipeline

//Why FluentValidation?

//Without FluentValidation:
//validation code gets repeated inside handlers

//Example

if (string.IsNullOrWhiteSpace(request.Category))

if (request.Amount <= 0)

//This makes handler messy and hard to maintain

//------------------------------------------------------------

//Goal

//Separate:
//1. Validation logic
//2. Business logic

//------------------------------------------------------------

//Architecture Flow

Controller
    ->
Mediator
    ->
ValidationBehavior
    ->
Validator
    ->
Handler
    ->
Service
    ->
Database

//------------------------------------------------------------

//Install packages

//Application Layer

dotnet add package FluentValidation

dotnet add package FluentValidation.DependencyInjectionExtensions

//------------------------------------------------------------

//Validator class

//Validator contains only validation rules

using Expense.Application.Commands;
using FluentValidation;

namespace Expense.Application.Commands;

public class CreateExpenseCommandValidator
    : AbstractValidator<CreateExpenseCommand>
{
    public CreateExpenseCommandValidator()
    {
        RuleFor(x => x.Request.Category)
            .NotEmpty()
            .WithMessage("Category is required");

        RuleFor(x => x.Request.Amount)
            .GreaterThan(0)
            .WithMessage("Amount must be greater than zero");

        RuleFor(x => x.Request.UserId)
            .GreaterThan(0)
            .WithMessage("User id must be greater than zero");
    }
}

//------------------------------------------------------------

//ValidationBehavior

//ValidationBehavior automatically executes validators
//before handler execution

using FluentValidation;
using MediatR;
using Shared.Exceptions;

namespace Expense.Application.Validator;

public class ValidationBehavior<TRequest, TResponse>
    : IPipelineBehavior<TRequest, TResponse>
    where TRequest : IRequest<TResponse>
{
    private readonly IEnumerable<IValidator<TRequest>> _validators;

    public ValidationBehavior(
        IEnumerable<IValidator<TRequest>> validators
    )
    {
        _validators = validators;
    }

    public async Task<TResponse> Handle(
        TRequest request,
        RequestHandlerDelegate<TResponse> next,
        CancellationToken cancellationToken
    )
    {
        var context = new ValidationContext<TRequest>(request);

        var validationTasks = _validators
            .Select(v => v.ValidateAsync(context, cancellationToken))
            .ToArray();

        var validationResults = await Task.WhenAll(validationTasks);

        var failures = validationResults
            .SelectMany(result => result.Errors)
            .Where(f => f != null)
            .ToList();

        if (failures.Count != 0)
        {
            var errorMessages = string.Join(
                "; ",
                failures.Select(f => f.ErrorMessage)
            );

            throw new BadRequestException(
                "Validation Failed",
                errorMessages,
                "VALIDATION_ERROR"
            );
        }

        return await next();
    }
}

//------------------------------------------------------------

//Register FluentValidation in Program.cs

using FluentValidation;
using Expense.Application.Validator;

builder.Services.AddValidatorsFromAssembly(
    Assembly.Load("Expense.Application")
);

builder.Services.AddTransient(
    typeof(IPipelineBehavior<,>),
    typeof(ValidationBehavior<,>)
);

//------------------------------------------------------------

//What AddValidatorsFromAssembly does

//Scans Expense.Application project
//and automatically registers all validators

//Example

CreateExpenseCommandValidator

//------------------------------------------------------------

//What IPipelineBehavior does

//Before handler execution,
//MediatR automatically runs ValidationBehavior

//------------------------------------------------------------

//Actual runtime flow

//Step 1

Controller
    ->
_mediator.Send(new CreateExpenseCommand())

//------------------------------------------------------------

//Step 2

Mediator receives request

//------------------------------------------------------------

//Step 3

Mediator sees ValidationBehavior pipeline

//------------------------------------------------------------

//Step 4

ValidationBehavior finds matching validator

CreateExpenseCommandValidator

//------------------------------------------------------------

//Step 5

Validator rules execute

RuleFor(x => x.Request.Amount)

RuleFor(x => x.Request.Category)

//------------------------------------------------------------

//Step 6

If validation fails

throw BadRequestException(...)

//Handler never executes

//------------------------------------------------------------

//Step 7

If validation succeeds

return await next();

//This moves request to actual handler

//------------------------------------------------------------

//Final Flow

HTTP Request
    ->
Controller
    ->
Mediator
    ->
ValidationBehavior
    ->
Validator
    ->
Handler
    ->
Service
    ->
Repository
    ->
Database

//------------------------------------------------------------

//Benefits

//1. Cleaner handlers
//2. Centralized validation
//3. Reusable validation rules
//4. Better scalability
//5. Enterprise CQRS architecture
//6. Invalid requests stop before business logic

//------------------------------------------------------------

//Important understanding

//Validator
//Used for request/input validation

//Examples:
//1. Empty string
//2. Invalid amount
//3. Required field
//4. Max length

//------------------------------------------------------------

//Handler
//Used for business validation

//Examples:
//1. User exists?
//2. Record already exists?
//3. Account active?

//These usually require DB/API calls

//------------------------------------------------------------

//Example understanding

//Validator

Amount > 0

//Business logic

Check whether UserId exists in database

//------------------------------------------------------------

//Handler becomes clean

//Without FluentValidation

Handler
    ->
Validation code
    ->
Business logic

//With FluentValidation

Validator
    ->
Validation logic

Handler
    ->
Business logic only

//------------------------------------------------------------

//RabbitMQ Message Queue Implementation

//------------------------------------------------------------

//Why RabbitMQ?

//Without RabbitMQ

Expense Service
    ->
Notification Service API Call

Problem:

If Notification Service is down

Expense Creation Fails

Expense Service becomes tightly coupled
to Notification Service

//------------------------------------------------------------

//With RabbitMQ

Expense Service
    ->
RabbitMQ Queue
    ->
Notification Service

Expense Service and Notification Service
are completely independent

//------------------------------------------------------------

//Real World Example

User creates expense

Expense Service saves expense

Expense Service publishes event

ExpenseCreatedEvent

RabbitMQ stores event

Notification Service consumes event

Notification record inserted

//------------------------------------------------------------

//Architecture

Expense Service
    |
    | Publish
    v
RabbitMQ
(expense-created)
    |
    | Consume
    v
Notification Service
    |
    v
Notification Database

//------------------------------------------------------------

//Producer

Expense Service

//------------------------------------------------------------

//Producer Responsibility

Publish message into queue

Producer does NOT know

1. Who consumes
2. When consumed
3. How many consumers exist

Producer only publishes

//------------------------------------------------------------

//Producer Runtime Flow

HTTP Request

POST /api/expenses

    ->
Controller

    ->
Mediator

    ->
CreateExpenseCommandHandler

    ->
ExpenseService

    ->
Database Save

    ->
RabbitMqPublisher

    ->
RabbitMQ Queue

//------------------------------------------------------------

//Published Event

ExpenseCreatedEvent

{
    ExpenseId,
    UserId,
    Category,
    Amount,
    CreatedAt
}

//------------------------------------------------------------

//Serialization

RabbitMQ cannot store C# object

Object

    ->
JSON

    ->
UTF8 Bytes

    ->
Queue

//------------------------------------------------------------

//Example

ExpenseCreatedEvent

    ->
JsonSerializer.Serialize()

    ->
{
   "ExpenseId":1,
   "UserId":10,
   "Amount":500
}

    ->
UTF8 Bytes

    ->
RabbitMQ Queue

//------------------------------------------------------------

//Queue

Queue Name

expense-created

//------------------------------------------------------------

//Queue Purpose

Stores messages until consumer processes them

//------------------------------------------------------------

//Queue Behaviour

Producer publishes

Message enters queue

Consumer offline

Message stays in queue

Consumer starts later

Message gets processed

//------------------------------------------------------------

//Consumer

Notification Service

//------------------------------------------------------------

//Problem

Notification Service has no HTTP request

Who will continuously watch queue?

Solution

Hosted Service

//------------------------------------------------------------

//AddHostedService

builder.Services.AddHostedService<
    ExpenseCreatedConsumer>();

//Registers background worker

//------------------------------------------------------------

//BackgroundService

Microsoft base class for long-running tasks

Examples

1. RabbitMQ Consumer
2. Email Worker
3. Scheduled Jobs
4. Cache Refresh Worker

//------------------------------------------------------------

//Application Startup Flow

Notification API Starts

    ->
ASP.NET Core Startup

    ->
AddHostedService

    ->
Create ExpenseCreatedConsumer

    ->
ExecuteAsync()

    ->
RabbitMQ Connection

    ->
Start Listening

//------------------------------------------------------------

//ExecuteAsync Purpose

Runs automatically
when application starts

Used for infinite listening

//------------------------------------------------------------

//Consumer Runtime Flow

RabbitMQ Queue

    ->
Message Arrives

    ->
ReceivedAsync Event Fires

    ->
Read Bytes

    ->
Convert To JSON

    ->
Deserialize

ExpenseCreatedEvent

    ->
NotificationService

    ->
Database Insert

//------------------------------------------------------------

//Deserialization Flow

Queue Bytes

    ->
UTF8 String

    ->
JSON

    ->
ExpenseCreatedEvent

//------------------------------------------------------------

//Acknowledgement

After successful processing

BasicAckAsync()

//Tells RabbitMQ

Message processed successfully

Remove message from queue

//------------------------------------------------------------

//Without Ack

Message remains in queue

Can be processed again

//------------------------------------------------------------

//Queue Counts

Ready

Messages waiting

//------------------------------------------------------------

//Unacked

Consumer received

But not acknowledged yet

//------------------------------------------------------------

//Example

Ready = 5

Consumer processes

Ready = 0

Unacked = 0

//------------------------------------------------------------

//IServiceScopeFactory

Consumer is Hosted Service

Hosted Service Lifetime

Singleton

//------------------------------------------------------------

//Problem

NotificationService

Registered as Scoped

Cannot inject Scoped
inside Singleton

//------------------------------------------------------------

//Solution

Create Scope

using var scope =
    _scopeFactory.CreateScope();

var notificationService =
    scope.ServiceProvider
         .GetRequiredService<
             INotificationService>();

//Fresh Scoped Service Created

//------------------------------------------------------------

//Final End-To-End Flow

User

    ->
POST /api/expenses

    ->
Expense Controller

    ->
Mediator

    ->
CreateExpenseCommandHandler

    ->
ExpenseService

    ->
PostgreSQL

    ->
RabbitMqPublisher

    ->
RabbitMQ Queue

expense-created

    ->
ExpenseCreatedConsumer

    ->
Deserialize Event

    ->
NotificationService

    ->
Notification Table

//------------------------------------------------------------

//Benefits

1. Loose Coupling

2. Asynchronous Communication

3. Better Scalability

4. Retry Capability

5. Fault Tolerance

6. Independent Services

7. Event Driven Architecture

//------------------------------------------------------------

//Important Understanding

Controller

Waits for HTTP Requests

//------------------------------------------------------------

//Hosted Service

Waits for Background Events

//------------------------------------------------------------

//RabbitMQ Queue

Temporary Message Storage

//------------------------------------------------------------

//Producer

Publishes Messages

//------------------------------------------------------------

//Consumer

Processes Messages

//------------------------------------------------------------

//Event

Represents Something That Happened

Example

ExpenseCreatedEvent

//------------------------------------------------------------